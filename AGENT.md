# Agent guidance

See [CLAUDE.md](CLAUDE.md) — canonical guidance for coding agents working in
this repo, kept in one place to avoid the two files drifting apart.

Short version: Conventional Commits, work on a feature branch merged via PR
(never commit to `main` directly), prefer `git worktree add` over switching
the current checkout's branch when starting a new feature.
